const citiesInitialState = null
export const cities = (state = citiesInitialState, action) => {
  switch (action.type) {
    case "ACTION_TYPE_1":
      return state
    default:
      return state
  }
}